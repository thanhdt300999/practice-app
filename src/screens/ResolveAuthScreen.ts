import React, { useEffect, useContext } from 'react';

const ResolveAuthScreen = () => {
 
  useEffect(() => {
    // tryLocalSignin();
  }, []);

  return null;
};

export default ResolveAuthScreen;
